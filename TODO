Change layer refiner to be heirarchical

Fix the window resizing code

Fix the RenderToTexture
Fix UpdateTexture

Add plane fitting function
Add function to take plane sweep results and create non-fronto-parallel plane

Layer Refiner seems to work on only 1 image size?

# Write a video writer w/ metadata
# Incorporate theia & Ceres (lite / mac only / separate build components)

# Add mesh viewer (trackball)
